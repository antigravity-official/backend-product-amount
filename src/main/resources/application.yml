server:
  port: 8089

spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false #OSIV 설정

logging:
  config: "classpath:logback-${spring.profiles.active}.xml" #Profile 별 로그 설정